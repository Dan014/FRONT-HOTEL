export interface Reserv {
    id?: number;
    first_name: string;
    last_name: string;
    email: string;
    init_date: Date;
    finish_date: Date;
    persons: number;
    boys: number;
    note: string;
}